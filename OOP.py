class Student;

lowered_loan = 0.245
num_of_students = 0 

def __info__(self, first, last, loan):
  self.first = first
  self.last = last
  self.loan = loan
  self.email = first + '.' + last + '@edu.com'

Student.num_of_students += 1 

def fullname(self):
  return '{} {}'.format(self.first, self.last)

def apply_loweredloan(self):
  self.loan = int(self.loan * lowered_loan)

stud_1 = Student('Anabel', 'Alexander', 50000)
stud_2 = Student('testing this', 'user', 509900)
